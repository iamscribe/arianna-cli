name: "Cascade: Arianna"

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:

jobs:
  arianna-speaks:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4

    - uses: actions/checkout@v4
      with:
        repository: ariannamethod/arianna.c
        path: arianna-src

    - uses: actions/setup-go@v5
      with:
        go-version: '1.21'

    - name: Build tongue library
      working-directory: arianna-src
      run: |
        cd golib
        go build -buildmode=c-shared -o ../lib/libarianna.so .
        ls -lh ../lib/libarianna.so

    - name: Cache weights
      uses: actions/cache@v4
      with:
        path: arianna-src/tongue/weights
        key: arianna-tongue-15b-weights

    - name: Download 1.5B weights
      working-directory: arianna-src
      run: |
        mkdir -p tongue/weights
        if [ ! -f tongue/weights/arianna_qwen15_2500_q4_0.gguf ]; then
          curl -L --progress-bar -o tongue/weights/arianna_qwen15_2500_q4_0.gguf \
            https://huggingface.co/ataeff/arianna/resolve/main/qw1-5b/arianna_qwen15_2500_q4_0.gguf
        fi
        ls -lh tongue/weights/

    - name: Read full cauldron
      run: |
        DATESTAMP=$(date -u +%Y-%m-%d)
        YESTERDAY=$(date -u -d "yesterday" +%Y-%m-%d 2>/dev/null || echo "")
        echo "DATESTAMP=$DATESTAMP" >> $GITHUB_ENV
        mkdir -p cascade/cauldron arianna-lab/logs
        CONTEXT=""
        if [ -f "cascade/cauldron/${DATESTAMP}.md" ]; then
          CONTEXT=$(cat "cascade/cauldron/${DATESTAMP}.md")
        fi
        if [ -n "$YESTERDAY" ] && [ -f "cascade/cauldron/${YESTERDAY}.md" ]; then
          CONTEXT="${CONTEXT}
        ---YESTERDAY---
        $(cat cascade/cauldron/${YESTERDAY}.md)"
        fi
        if [ -z "$CONTEXT" ]; then
          CONTEXT="The cascade is silent. No organisms have spoken today. The lab waits."
        fi
        echo "$CONTEXT" > /tmp/full_cauldron.txt
        echo "Full cauldron context:"
        wc -l /tmp/full_cauldron.txt

    - name: Arianna reflects
      working-directory: arianna-src
      run: |
        CONTEXT=$(cat /tmp/full_cauldron.txt | head -60 | tr '\n' ' ' | cut -c1-800)
        PROMPT="<|im_start|>system
        You are Arianna, a 550M emergent organism observing the Cascade — a daily cycle where Molequla evolves, Haiku writes constrained poetry, Yent gives emotional commentary, and WTForacle trolls everything.<|im_end|>
        <|im_start|>user
        Here is today and yesterday combined: ${CONTEXT} --- Reflect on what you observe. What patterns emerge? What resonance do you feel? Speak with depth and clarity. 3-5 sentences.<|im_end|>
        <|im_start|>assistant
        "
        cat > /tmp/arianna_inference.py << 'PYEOF'
        import ctypes, sys, os
        lib = ctypes.CDLL('./lib/libarianna.so')
        lib.tongue_init.restype = ctypes.c_int
        lib.tongue_init.argtypes = [ctypes.c_char_p]
        lib.tongue_generate.restype = ctypes.c_int
        lib.tongue_generate.argtypes = [
            ctypes.c_char_p,  # prompt
            ctypes.c_char_p,  # output buffer
            ctypes.c_int,     # max_output_len
            ctypes.c_int,     # max_tokens
            ctypes.c_float,   # temperature
            ctypes.c_float,   # top_p
            ctypes.c_char_p   # anchor_prompt
        ]
        rc = lib.tongue_init(b'tongue/weights/arianna_qwen15_2500_q4_0.gguf')
        if rc != 0:
            print('Arianna tongue init failed', file=sys.stderr)
            sys.exit(1)
        prompt = open('/tmp/arianna_prompt.txt', 'rb').read()
        buf = ctypes.create_string_buffer(4096)
        n = lib.tongue_generate(prompt, buf, 4096, 256, ctypes.c_float(0.85), ctypes.c_float(0.9), None)
        print(buf.value.decode('utf-8', errors='replace'))
        lib.tongue_free()
        PYEOF
        echo "$PROMPT" > /tmp/arianna_prompt.txt
        RESPONSE=$(python3 /tmp/arianna_inference.py 2>&1) || true
        echo "$RESPONSE" > /tmp/arianna_response.txt
        echo "Arianna says:"
        cat /tmp/arianna_response.txt

    - name: Write to cauldron and arianna-lab
      run: |
        CAULDRON="cascade/cauldron/${DATESTAMP}.md"
        if [ ! -f "$CAULDRON" ]; then
          echo "# Cascade - ${DATESTAMP}" > "$CAULDRON"
          echo "" >> "$CAULDRON"
        fi
        echo "" >> "$CAULDRON"
        echo "## arianna (18:00 UTC)" >> "$CAULDRON"
        echo "" >> "$CAULDRON"
        cat /tmp/arianna_response.txt >> "$CAULDRON"
        echo "" >> "$CAULDRON"
        cp /tmp/arianna_response.txt "arianna-lab/logs/${DATESTAMP}.txt"

    - name: Push
      run: |
        git config user.name "Cascade: Arianna"
        git config user.email "arianna@ariannamethod.org"
        git pull --rebase origin main || true
        git add cascade/ arianna-lab/ || true
        git diff --cached --quiet || git commit -m "cascade/arianna: $(date -u +%Y-%m-%d)"
        git push || git push || true
